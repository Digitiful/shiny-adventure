
# Digitiful // Master Node

Elite Digital Engineering & Technical Anomaly Vault.

## ⚠️ CRITICAL: CLEAN TRANSFER PROTOCOL (Fix ZIP Corruption)
If you are downloading this project to move it or import it elsewhere, you **MUST** follow these steps to avoid the "Corrupted ZIP" error:

1.  **DELETE `node_modules`**: This folder contains thousands of small files that crash ZIP tools.
2.  **DELETE `.next`**: This is the build cache and is not needed for transfers.
3.  **ZIP THE REST**: After deleting those two folders, ZIP your project. It will be small, fast, and 100% reliable.
4.  **RESTORE**: Once moved, simply run `npm install` in your terminal to get everything back.

---

## 🚀 LAUNCH PROTOCOL (Deploy Today)

Follow these steps in your Mac Terminal to establish the live link.

### 1. Terminal Ignition (Fix "Not a Git Repository")
If you see a Git error, execute these commands in this exact order:
```bash
# 1. Initialize the repository
git init

# 2. Stage all assets
git add .

# 3. Commit to the local node
git commit -m "Launch Protocol: Ignition Initialized"

# 4. Link to your GitHub (Replace with your actual repo URL)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# 5. Push to the cloud
git branch -M main
git push -u origin main
```

### 2. Connection Verification
To verify the link is active, run:
```bash
git remote -v
```
**Expected Output:** Your GitHub URL mapped to `origin`.

### 3. Antigravity Ignition (App Hosting)
1. Go to the [Firebase Console](https://console.firebase.google.com/).
2. Select your project: **charged-scholar-475920-i9**.
3. Navigate to **Build > App Hosting** and click **Get Started**.
4. Connect your GitHub repository and follow the wizard.

---
**[ SYSTEM STATUS: CALIBRATED FOR CLEAN TRANSFER ]**
